package utils;

/**
 * Type of operations that are allowed in this program among client-server and server-server communication.
 */
public enum Type {
  GET, PUT, DELETE, PROMISE, ACCEPT_REQUEST, SAVE, ACCEPT_RESPONSE
}
